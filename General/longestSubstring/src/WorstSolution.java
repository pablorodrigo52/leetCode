public class WorstSolution {
 
    
    private static void lengthOfLongestSubstring(String s) {
        // for i : s.length
            // for j : s+1.length
                // if (s[i] != s[j]) 
                    // result += s[j]
                // else
                    // max = Math.max(result.length(), j)
                    // clearResult();
                    // break;

    }
}
